/*
package Franco.Parcial2022_02.Ejercicio3;

import java.time.LocalDate;
import java.util.Date;

public class Loan {
    double amount;
    LocalDate fecha;
    int cantCuotas;
    Destinatario dest;

    Loan(double amount,LocalDate fecha, int cantCuotas, Destinatario dest) throws LoanException {
        if (!dest.isValid(amount,cantCuotas)){
            throw new LoanException("Invalid Loan.");
        }
        this.amount=amount;
        this.fecha=fecha;
        this.cantCuotas=cantCuotas;
        this.dest=dest;
    }
    void pay(int numCuota,LocalDate fechaDePago)throws LoanException{

    }
}
*/
